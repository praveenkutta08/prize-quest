// Root ESLint flat config — delegates to the shared @pq/eslint-config package
// (mirrors CDP-UI). One repo-wide pass enforces the import boundaries.
import config from "@pq/eslint-config";

export default config;
