// pq-address-form has no local enums; the form shape is `AddressData` from
// @pq/contracts. On a valid OK the form fires `pq-address-submit`
// (detail = AddressData); Back fires `pq-back` (no detail payload).
export type {};
